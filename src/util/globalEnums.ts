export enum Environments {
  "DEVELOPEMNT" = "developemnt",
  "PRODUCTION" = "production",
}
export enum ConsoleModes {
  "LOG" = "log",
  "INFO" = "info",
  "DEBUG" = "debug",
}
export enum ConsoleTagTypes {
  "ALL" = "all",
  "TAGS" = "tags",
}
export enum ConsoleTags {
  "MIDDLEWARE" = "middleware",
  "ENTITY" = "entity",
  "AUTH" = "auth",
}
